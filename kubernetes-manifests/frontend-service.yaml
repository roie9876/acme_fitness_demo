apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: acmefit
    service: frontend
spec:
  type: LoadBalancer
  ports:
    - name: http-frontend
      protocol: TCP
      port: 3000
#      nodePort: 30430
  selector:
    app: acmefit
    service: frontend

